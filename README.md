# tangleflow-coverage

## Running

```sh
npm start
```

Finds repos with a `.github` folder but no `.tangled` folder, gets the workflows and runs tangleflow.
Used to get an overview of which errors/limitations are the most common + which repos are ready to convert.
